package Repeat;

//введение в интерфейсы и их реализацию в классах
public interface IShape {
    //в интерфейсах методы только описываются, но не реализуются
    //реализация методов осуществляется в классах, которые реализуют интерфейс
    double getArea();
    double getPer();
}
